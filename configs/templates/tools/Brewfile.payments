##
# brew bundle --upgrade --upgrade-formulae --install --jobs=auto --file=Brewfile.payments
##

#-------------------------------------------------------------------------------
# TAPS

# @config-manager:start taps
tap "stripe/stripe-cli", trusted: true
# @config-manager:end taps

#-------------------------------------------------------------------------------
# FORMULAE

# @config-manager:start brew
# https://docs.stripe.com/stripe-cli
brew "stripe/stripe-cli/stripe"
# @config-manager:end brew
