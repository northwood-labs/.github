##
# brew bundle --upgrade --upgrade-formulae --install --jobs=auto --file=Brewfile.containers
##

#-------------------------------------------------------------------------------
# TAPS

# @config-manager:start taps
tap "slp/krunvm", trusted: true if OS.mac?
# @config-manager:end taps

#-------------------------------------------------------------------------------
# FORMULAE

# @config-manager:start brew
# https://github.com/google/go-containerregistry/tree/main/cmd/crane
brew "crane"

# https://github.com/slp/krunvm
brew "slp/krunvm/krunvm" if OS.mac?
# @config-manager:end brew
