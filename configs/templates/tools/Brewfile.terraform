##
# brew bundle --upgrade --upgrade-formulae --install --jobs=auto --file=Brewfile.terraform
##

#-------------------------------------------------------------------------------
# TAPS

# @config-manager:start taps
tap "terraform-linters/tap", trusted: true
# @config-manager:end taps

#-------------------------------------------------------------------------------
# FORMULAE

# @config-manager:start brew
brew "tflint"
# @config-manager:end brew
